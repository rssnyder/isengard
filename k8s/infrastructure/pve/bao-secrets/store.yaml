apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: bao
  namespace: external-secrets
spec:
  provider:
    vault:
      server: "http://bao.r.ss:8200"
      path: "pvekv"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "pve"
