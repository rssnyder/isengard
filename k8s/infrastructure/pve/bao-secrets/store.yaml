apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: bao
  namespace: external-secrets
spec:
  provider:
    vault:
      server: "http://192.168.2.147:8199"
      path: "pvekv"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "pve"
          serviceAccountRef:
            audiences:
            - vault
