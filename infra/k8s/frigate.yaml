---
apiVersion: v1
kind: Namespace
metadata:
  name: frigate

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: ata-frigate
  namespace: frigate
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1000Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/pale/frigate"

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ata-frigate
  namespace: frigate
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: ata-frigate
  storageClassName: manual
